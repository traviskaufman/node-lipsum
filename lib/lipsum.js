// Generated by CoffeeScript 1.3.3
/*
# Lorem Ipsum Wrapper
*/

var Lipsum, Parser, Service, events,
  __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

Service = require('./service');

Parser = require('./parser');

events = require('events');

Lipsum = (function(_super) {
  var _this = this;

  __extends(Lipsum, _super);

  function Lipsum() {
    this.serviceCallback = __bind(this.serviceCallback, this);

    this.getText = __bind(this.getText, this);
    this._parser = new Parser();
    this._service = new Service();
    this.text = null;
  }

  Lipsum.prototype.defaults = {
    amount: 5,
    what: 'paras',
    start: 'no'
  };

  Lipsum.prototype.getText = function(callback, opts) {
    var prop, _i, _len, _ref;
    if (!opts) {
      opts = this.defaults;
    } else {
      _ref = this.defaults;
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        prop = _ref[_i];
        if (opts[prop] == null) {
          opts[prop] = this.defaults[prop];
        }
      }
    }
    this._service.get("json", this.serviceCallback, opts);
    return this.on('lipsum', function(txt) {
      return callback(txt);
    });
  };

  Lipsum.prototype.parserCallbacks = {
    success: function(result) {
      return Lipsum.emit('lipsum', result.lipsum);
    },
    error: function(err) {
      throw new Error("Parser Error: " + err.message);
    }
  };

  Lipsum.prototype.serviceCallback = function(text) {
    console.dir(text);
    console.dir(JSON.parse(text));
    return this._parser.feed(text).parse(this.parserCallbacks.success, this.parserCallbacks.error);
  };

  return Lipsum;

}).call(this, events.EventEmitter);

if (typeof module !== "undefined" && module !== null) {
  module.exports = Lipsum;
}
